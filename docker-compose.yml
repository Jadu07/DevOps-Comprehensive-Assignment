services:
  app:
    build: .
    ports:
      - "80:5000"
    depends_on:
      - mongo
    environment: 
      - MONGO_URI:mongodb://mongo:27017/mydb

  mongo:
    image: mongo:7
    volumes: 
      - mongo_data:/data/db

volumes:
  mongo_data:




